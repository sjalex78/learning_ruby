require 'tennis'

RSpec.describe Tennis do
  it 'what it does' do
    expect(Tennis.score).to eq 'expected outcome'
  end
  it 'what it does' do
    expect(Tennis.score).to eq 'expected outcome'
  end
  it 'what it does' do
    expect(Tennis.score).to eq 'expected outcome'
  end
  it 'what it does' do
    expect(Tennis.score).to eq 'expected outcome'
  end
end
